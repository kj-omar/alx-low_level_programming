#include "main.h"
#include<stdio.h>

void _puts(char *str)
{
	printf("%s\n", str);
}
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
